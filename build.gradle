allprojects {
    apply plugin: 'idea'
    group 'com.thingslink'
    version '1.0.0'
    repositories {
        mavenLocal()
        mavenCentral()
    }
}


subprojects {
    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }
}
